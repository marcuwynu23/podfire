# PodFire autofile — run `auto dev` to start all dev servers
# Requires: auto cli found in https://github.com/marcuwynu23/Auto/releases
# Each + block runs in its own terminal.

dev {
    + cd app && pnpm install && pnpm dev:all
    + cd agent && pnpm install && pnpm dev
}
